neo4j-agent-memory[openai]==0.5.0
# The library's bolt connect path imports its nams module, which needs httpx
# but does not declare it outside the hosted-service extra (verified from the
# 0.5.0 deploy traceback: _connect_bolt -> nams -> transport -> import httpx).
httpx>=0.27,<1
fastapi>=0.115,<1
uvicorn>=0.30,<1
PyJWT>=2.9,<3
